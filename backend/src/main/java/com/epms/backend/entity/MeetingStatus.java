package com.epms.backend.entity;

public enum MeetingStatus {
    PENDING,
    ACCEPTED,
    RESCHEDULE_REQUESTED,
    RESCHEDULE_MGR,
    CANCEL_REQUESTED,
    DECLINED,
    ONGOING,
    COMPLETED,
    CANCELLED
}
