package com.epms.backend.entity;

public enum MeetingStatus {
    PENDING,
    ACCEPTED,
    RESCHEDULE_REQUESTED,
    RESCHEDULE_MGR,
    CANCEL_REQUESTED,
    ONGOING,
    COMPLETED,
    CANCELLED
}
